# 2026-07-28 工作日志

## A股题材碗底形态分析
- 任务：按"碗底形状"筛选处于底部区间的题材/板块。
- 数据：重建 /tmp/sw1_sectors.json(31 申万一级) + /tmp/concept_sectors.json(47 聚源产业概念)。用 fetch_fresh_klines.py 重新拉取 250 日 K 线（重试+与旧数据合并兜底），78 个样本 K 线更新至 2026-07-28（66 个全新，12 个用 07-22 旧数据兜底）。
- 算法：bowl_bottom_enhanced.py（区间位置 + 二次曲线 U 形拟合 + 双窗口趋势减速 + 低点抬升 + 量/波幅压缩），100 分制，分级：确认碗底/确认中/减速筑底/低位盘整/下跌中继/观望。
- 结果（7 个确认碗底，均为全新数据）：钢铁88、商贸零售82、REITs82、一汽系74、农林牧渔70、美容护理67、交通运输64；减速筑底：振兴东北77、沪伦通65。55 个仍为"下跌中继"——大盘整体未到底。
- 产出：sector_bowl_report.html（含 K 线缩略图、TOP25 排名、详细判定），JS 已通过 node --check。
- 经验：westock-data `kline <板块code> --raw` 对板块偶发返回 []（瞬时错误），需重试；`sector list` 无 scope 时报错，无法枚举全概念板块，故沿用既有 78 样本。
- 注意：碗底≠必反弹，钢铁/商贸零售属长期下行结构型低位（价值陷阱风险）；农林牧渔/美容护理/交通运输近20日已转正，碗沿更可信。

## 收盘后复跑碗底分析（15:03）
- 收盘后重新拉取78板块K线：67个更新至07-28收盘价，11个沿用07-22兜底数据。
- 复跑 bowl_bottom_enhanced.py + generate_bowl_report.py，结果与盘中版基本一致：
  - 🟢确认碗底/确认中(7个)：钢铁88、REITs82、商贸零售77、一汽系74、农林牧渔70、美容护理67、交通运输64
  - 🟡减速筑底/低位盘整(3个)：供应链金融68(新入)、沪伦通65、振兴东北58
  - 🔴下跌中继55个 — 大盘整体仍未到底
- 与盘中版差异：商贸零售82→77(收盘回落)、供应链金融新进入低位盘整、REITs升至82。
- 产出覆盖 sector_bowl_report.html / bowl_bottom_enhanced.json / sector_kline_data.json。

## 全市场概念板块枚举+K线拉取（15:20）
- 发现 `sector list` 正确用法：不带参数列出6类清单(sw1/sw2/sw3/area/industry/style)，带scope分页枚举，如 `sector list concept_list_industry --limit 200 --offset N`。之前"报错"是误判。
- 全市场概念板块共 830 个：产业概念721 + 风格概念78 + 地域概念31。
- fetch_all_concepts.py：枚举+并行拉取250日K线（6并发、4重试）。首轮788/830成功，42个失败经二轮重试（6次重试、4并发）后全部成功 → 830/830 覆盖。
- 数据质量：829个更新至2026-07-28收盘；1个"C股(上市次日至五日)"停更于2025-12-16（死板块）；776个K线≥200根，54个不足200根（新概念）。
- 产出：all_concept_sectors.json（清单）、concept_kline_data.json（30.5MB K线）。
- 经验：板块kline接口瞬时失败率约5%，两轮重试即可100%覆盖，无需兜底旧数据。

## 将concept清单移入project skill（15:25）
- 将 all_concept_sectors.json（830个概念板块全量清单）从项目根目录移动到 .workbuddy/skills/bowl-bottom-sector-scanner/ 下，使该skill自包含。
- 更新 analyze.py 的 load_sectors()：概念板块优先读取skill本地的 all_concept_sectors.json（830个：产业721+风格78+地域31）；申万一级行业优先读 skill 本地 sw1_sectors.json，缺失回退 /tmp/sw1_sectors.json。验证共加载 861 个板块。
- 同步更新 SKILL.md：Step1 说明概念清单已内置、sw1 本地优先、刷新命令。
- 现在运行 skill 的 analyze.py 无需先拉 /tmp/concept_sectors.json，但 sw1_sectors.json 仍需在 skill 目录或 /tmp 中（/tmp 现有 31 个）。

## 全量碗底扫描（16:05）+ 修复覆盖
- 运行 .workbuddy/skills/bowl-bottom-sector-scanner/analyze.py 全量扫描 861 板块（830概念+31申万），耗时25分钟。
- 修复 analyze.py 的 K线拉取可靠性：fetch_kline 增加4次重试；并行拉取后增加"补拉 pass"（缺失板块再6次重试）。
- 首轮结果：848 板块有K线，但 13 个缺失（11概念+2申万）。改用单独增量补拉脚本（逐条保存，避免崩溃丢进度）将 861 全部补齐。
- 经验：8并发下 westock kline 接口偶发整批失败（13个），需降低并发或串行补拉；之前 analyze 脚本因大 json.dump + import 在内存压力下被 kill(137)，拆分 fetch 与 re-analyze 两步后稳定。
- 最终覆盖：830概念 100% 有K线（811已评分，19个因K线<80根无法评分=极新概念）；全样本评分842板块。
- 标签分布：🟢确认碗底32 + 确认中17 = 49个；🟡减速筑底11+低位盘整37；⚪观望131；🔴下跌中继614。
- 重分析补拉的13个：仅"钢结构概念"为低位盘整(66)，其余多下跌中继/观望；"国家大基金持股"分0。
- 产出：sector_kline_data.json(861)、bowl_bottom_results.json(842)、sector_bowl_report.html(全量)。

## 电信运营板块深度分析（sector-deep-analysis skill）（16:05）
- 任务：按 .workbuddy/skills/sector-deep-analysis 工作流生成《电信运营板块深度分析-20260728.html》。
- 板块定位：电信运营=聚源产业概念 pt02GN2324，6 只成分股：中国移动/中国电信/中国联通（运营商）、中国卫通（卫星）、东方明珠/广电网络（广电系）。
- ⚠️关键坑：westock-data 所有命令（quote/kline/finance/consensus/rating/report/macro）**必须加 dangerouslyDisableSandbox:true**，否则沙箱屏蔽网络返回"数据为空"/`[]`。且数据源高概率瞬时失败（quote 批量常整批 SKILL_004、kline/consensus 偶发 `[]`/`null`），必须用重试循环（见 /tmp/fetch_westock.py：最多10次重试，拒绝 success:false 与空数组/null）。
- 概念代码 pt02GN2324 不可直接取 K 线 → 改用 6 成分股 250 日 K 线自建等权/市值加权"电信运营概念指数"。通信 SW1（pt01801770）可正常取 K 线作对照。
- 核心结论（变异认知）：通信SW1 年内 +21%（AI硬件驱动），但三大运营商被当"衰退公用事业"定价（PE 15-18×、股息4-5%），与算力网核心主体地位严重错配；等权概念指数250日分位4.8%（极低位，含广电/卫星弱势），市值加权（运营商主导）250日分位48%、20日趋势+9.6%翻正→左侧企稳。评级：标配/超配（偏多·左侧）；运营商超配左侧、卫通主题关注、广电系回避。
- 数据：6股 quote/finance/consensus/rating/report 全拉取（广电网络无一致预期/评级覆盖）；macro core_indicators_cur 可用（社零/通讯器材），pmi/gdp 失败转 WebSearch。
- 产出：电信运营板块深度分析-20260728.html（85KB，含4张 ECharts：择时双线/子赛道背离/结构柱状/财务雷达），JS 已通过 node --check。
